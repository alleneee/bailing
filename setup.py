from setuptools import setup, find_packages

setup(
    name="bailing",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # 依赖项会从requirements.txt中读取
    ],
    author="寒江雪",
    author_email="",
    description="百聆语音助手",
    keywords="voice assistant, AI, chat",
    python_requires=">=3.8",
) 